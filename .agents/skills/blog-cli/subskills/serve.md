# `blog serve`

Starts local preview for generated output.

## Usage
```bash
blog serve
blog serve --port 9000
```

## Prerequisite
Run `blog build` first so `docs/` exists.

## Expected Output
`Preview available at http://localhost:<port> (serving <path>)`
