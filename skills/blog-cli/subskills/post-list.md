# `inkwell post list`

List current markdown posts under `content/posts/`.

## Usage
```bash
inkwell post list
inkwell post list --json
```

## Notes
- Human mode prints one filename per line.
- `--json` prints an array of filenames for scripting.
