# `inkwell serve`

Starts local preview for generated output.

## Usage
```bash
inkwell serve
inkwell serve --port 9000
```

## Prerequisite
Run `inkwell build` first so `docs/` exists.

## Expected Output
`Preview available at http://localhost:<port> (serving <path>)`
